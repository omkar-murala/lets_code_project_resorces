/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        // Add your image configuration options here
        domains: ["plus.unsplash.com","images.pexels.com","images.unsplash.com"], // Add allowed domains for external images
    },
};

export default nextConfig;
